---
// src/components/ProsCons.astro
import { Icon } from 'astro-icon/components';
---
<div class="pros-cons-grid">
  <div class="card pros-card">
    <div class="header">
      <Icon name="heroicons:check-circle" class="icon pros-icon" />
      <h4>ข้อดี (Pros)</h4>
    </div>
    <div class="content">
      <slot name="pros" />
    </div>
  </div>
  <div class="card cons-card">
    <div class="header">
      <Icon name="heroicons:x-circle" class="icon cons-icon" />
      <h4>ข้อจำกัด (Cons)</h4>
    </div>
    <div class="content">
      <slot name="cons" />
    </div>
  </div>
</div>

<style>
  .pros-cons-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
    margin: 2.5rem 0;
  }
  .card {
    padding: 0;
  }
  
  .header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--card-border-color);
  }
  .header h4 {
    margin: 0;
    font-size: 1.1rem;
    font-family: var(--font-sans);
    color: var(--font-color);
    font-weight: 600;
  }
  .icon { 
    font-size: 1.75rem;
    flex-shrink: 0;
  }
  .pros-icon { color: var(--success-color); } 
  .cons-icon { color: var(--danger-color); } 
  
  .content {
    padding: 1.25rem;
  }
  .content :global(ul) {
    padding-left: 1.25rem;
    margin: 0;
    line-height: 1.7;
    color: var(--font-color-light);
  }
  
  @media (max-width: 700px) {
    .pros-cons-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
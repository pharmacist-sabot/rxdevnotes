---
title: 'เลิกทำ Manual Release เปลี่ยนมาใช้ Semantic Release กันเถอะ'
description: 'คู่มือการตั้งค่าระบบ Automated Release บน GitHub Actions เปลี่ยนการจัดการเวอร์ชัน เขียน Changelog และติด Tag ที่ยุ่งยากให้เป็นเรื่องอัตโนมัติด้วย Semantic Release'
pubDate: '2025-12-24'
heroImage: './semantic-release-hero.png'
category: 'DevOps'
tags: ['CI/CD', 'GitHub Actions', 'Automation', 'Semantic Release', 'Workflow']
featured: true
---

เคยไหมครับ? เขียนโค้ดเสร็จ ฟีเจอร์พร้อม แต่พอจะ Deploy ทีไรต้องมานั่งปวดหัวกับเรื่องพวกนี้
1.  ต้องมานั่งนึกว่า "เอ๊ะ รอบนี้แก้ bug หรือเพิ่มฟีเจอร์นะ? ต้องขึ้น version ไหนดี?"
2.  ต้องมานั่งไล่อ่าน Commit ย้อนหลังเพื่อเขียน Changelog
3.  ต้องรันคำสั่ง `npm version`, `git tag`, `git push` เองทุกครั้ง
4.  บางทีลืมอัปเดตไฟล์ `package.json` ให้ตรงกับ Tag อีก

ถ้าคุณเบื่อวงจรนี้ ผมขอแนะนำให้รู้จักกับ **Semantic Release** เครื่องมือที่จะทำให้กระบวนการปล่อยซอฟต์แวร์ของคุณเป็นอัตโนมัติ 100% เพียงแค่คุณ **"เขียน Commit Message ให้ถูกหลัก"** เท่านั้น

---

## Semantic Release คืออะไร?

Semantic Release คือเครื่องมือที่ช่วยจัดการกระบวนการปล่อยซอฟต์แวร์ (Version Management & Package Publishing) แบบอัตโนมัติ โดยมันจะตัดสินใจทำสิ่งต่างๆ แทนเราโดยดูจาก **Commit Message**

หลักการทำงานของมันคือ:
1.  **Analyze Commits:** อ่านประวัติการ commit ตั้งแต่ release ครั้งล่าสุด
2.  **Determine Version:** คำนวณเลขเวอร์ชันถัดไปตามหลัก **SemVer** (Major.Minor.Patch)
3.  **Generate Notes:** สร้าง Release Notes (Changelog) สรุปสิ่งที่เปลี่ยนแปลง
4.  **Publish:** ปล่อยเวอร์ชันใหม่ (สร้าง Git Tag, Release บน GitHub, หรือ Publish ขึ้น NPM)

<FeatureGrid>
  <FeatureCard title="อัตโนมัติทุกขั้นตอน" icon="cog-6-tooth">
    ไม่ต้องกด Tag เอง ไม่ต้องแก้ package.json เอง ระบบจัดการให้หมดเมื่อ Code ถูก Merge เข้า Main branch
  </FeatureCard>
  <FeatureCard title="Changelog สวยงาม" icon="document-text">
    สร้างไฟล์ CHANGELOG.md และ Release Note บน GitHub ให้อัตโนมัติ พร้อมแบ่งหมวดหมู่ Feature, Bug Fix ให้เสร็จสรรพ
  </FeatureCard>
  <FeatureCard title="SemVer ที่ถูกต้อง" icon="check-badge">
    หมดปัญหาเรื่องมนุษย์คำนวณเวอร์ชันผิด ระบบจะดูตามกฎที่ตั้งไว้เป๊ะๆ ว่าต้องขยับเลขไหน
  </FeatureCard>
  <FeatureCard title="CI/CD Friendly" icon="server-stack">
    ออกแบบมาเพื่อรันบน CI Server (เช่น GitHub Actions) โดยเฉพาะ ไม่ต้องรันบนเครื่องตัวเอง
  </FeatureCard>
</FeatureGrid>

---

## หัวใจสำคัญ: Conventional Commits

เพื่อให้ Semantic Release ทำงานได้ เราต้องตกลงกันก่อนว่าจะเขียน Commit Message ในรูปแบบมาตรฐานที่เรียกว่า **Conventional Commits**

รูปแบบ: `<type>(<scope>): <subject>` (ส่วน `<scope>` จะใส่หรือไม่ก็ได้)

| Type | ความหมาย | ผลลัพธ์ต่อ Version |
| :--- | :--- | :--- |
| **fix** | แก้ไขบั๊ก | **Patch** (0.0.x) |
| **feat** | เพิ่มฟีเจอร์ใหม่ | **Minor** (0.x.0) |
| **perf** | ปรับปรุงประสิทธิภาพ | **Patch** (0.0.x) |
| **BREAKING CHANGE** | การเปลี่ยนแปลงที่กระทบของเดิม | **Major** (x.0.0) |
| **chore, docs, style** | งานจิปาถะ, เอกสาร | ไม่เปลี่ยน Version (หรือ Config ให้เป็น Patch ได้) |

<InfoBox title="ตัวอย่าง Commit" type="info">
  <ul>
    <li><code>fix(auth): login button not working</code> -> อัปเดต Patch</li>
    <li><code>feat(blog): add dark mode support</code> -> อัปเดต Minor</li>
    <li><code>feat(api): change response format \n\n BREAKING CHANGE: api structure changed</code> -> อัปเดต Major</li>
  </ul>
</InfoBox>

<InfoBox title="ระวัง! กับดักของ Squash Merge" type="warning">
  ถ้าทีมของคุณใช้การ Merge PR แบบ <strong>Squash and merge</strong> ต้องระวังให้ดีครับ <br/>
  GitHub จะใช้ <strong>Title ของ PR</strong> เป็น Commit Message หลักที่จะเข้าไปอยู่ใน Main branch <br/>
  ดังนั้นต้องตรวจสอบให้แน่ใจว่า Title ของ PR เขียนถูกตามหลัก Conventional Commits (เช่น <code>feat: add login page</code>) ไม่อย่างนั้น Semantic Release จะข้ามการปล่อยเวอร์ชันไปเลยเพราะมันอ่านไม่ออกครับ
</InfoBox>

---

## เริ่มต้นติดตั้ง (Setup)

เราจะมาติดตั้ง Semantic Release ในโปรเจกต์ Node.js โดยใช้ **GitHub Actions** เป็นตัวรันครับ

### 1. ติดตั้ง Dependencies

รันคำสั่งนี้ในโปรเจกต์ของคุณเพื่อติดตั้ง package ที่จำเป็นสำหรับการทำ Development

```bash
npm install -D semantic-release @semantic-release/changelog @semantic-release/git @semantic-release/github @semantic-release/npm conventional-changelog-conventionalcommits
```

<CodeExplainer title="อธิบาย Package แต่ละตัว">
  <details>
    <summary><code>semantic-release</code></summary>
    <p>ตัว Core หลักในการทำงาน</p>
  </details>
  <details>
    <summary><code>@semantic-release/changelog</code></summary>
    <p>Plugin สำหรับสร้างและอัปเดตไฟล์ <code>CHANGELOG.md</code></p>
  </details>
  <details>
    <summary><code>@semantic-release/git</code></summary>
    <p>Plugin สำคัญ ใช้สำหรับ Commit ไฟล์ที่ถูกแก้ไข (เช่น package.json ที่เปลี่ยนเลขเวอร์ชัน หรือ CHANGELOG.md) กลับเข้าไปใน Repo</p>
  </details>
  <details>
    <summary><code>conventional-changelog-conventionalcommits</code></summary>
    <p>Preset การเขียน Commit เพื่อให้ระบบเข้าใจรูปแบบที่เราใช้</p>
  </details>
</CodeExplainer>

### 2. สร้างไฟล์ Config

สร้างไฟล์ชื่อ `.releaserc.json` หรือ `.releaserc` ไว้ที่ Root ของโปรเจกต์ แล้วใส่ Config นี้ลงไปครับ:

```json
{
  "$schema": "https://json.schemastore.org/semantic-release.json",
  "branches": ["main"],
  "plugins": [
    [
      "@semantic-release/commit-analyzer",
      {
        "preset": "conventionalcommits",
        "releaseRules": [
          { "breaking": true, "release": "major" },
          { "type": "feat", "release": "minor" },
          { "type": "fix", "release": "patch" },
          { "type": "perf", "release": "patch" },
          { "type": "revert", "release": "patch" },
          { "type": "docs", "release": "patch" },
          { "type": "refactor", "release": "patch" },
          { "type": "chore", "scope": "deps", "release": "patch" }
        ]
      }
    ],
    [
      "@semantic-release/release-notes-generator",
      {
        "preset": "conventionalcommits",
        "presetConfig": {
          "types": [
            { "type": "feat", "section": "🚀 New Features" },
            { "type": "fix", "section": "🐛 Bug Fixes" },
            { "type": "perf", "section": "⚡ Performance" },
            { "type": "docs", "section": "📝 Documentation" },
            { "type": "chore", "section": "📦 Dependency Updates" },
            { "type": "refactor", "section": "♻️ Refactoring" }
          ]
        }
      }
    ],
    "@semantic-release/changelog",
    [
      "@semantic-release/npm",
      {
        "npmPublish": false
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["package.json", "package-lock.json", "CHANGELOG.md"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    "@semantic-release/github"
  ]
}
```

<InfoBox title="ข้อควรระวังเรื่อง npmPublish" type="warning">
  ใน config นี้ผมตั้งค่า <code>"npmPublish": false</code> ไว้ เพราะส่วนใหญ่เราทำเว็บ (Web Application) เราไม่ได้ต้องการเอาโค้ดเราไปปล่อยให้คนอื่นโหลดผ่าน <code>npm install</code> แต่เราต้องการแค่ให้มันอัปเดตเลขเวอร์ชันใน <code>package.json</code> เท่านั้นครับ
</InfoBox>

### 3. สร้าง GitHub Actions Workflow

สร้างไฟล์ `.github/workflows/release.yml` เพื่อสั่งให้ GitHub ทำงานเมื่อมีการ Push เข้ามาที่ branch `main`

```yaml
name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write # จำเป็นมาก เพื่อให้บอทสร้าง Release และ Tag ได้
      issues: write   # สำหรับคอมเมนต์ใน issues (ถ้ามี)
      pull-requests: write # สำหรับคอมเมนต์ใน PRs (ถ้ามี)

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # ดึงประวัติทั้งหมดมาเพื่อวิเคราะห์ Commit ย้อนหลัง
          persist-credentials: false # ปิดเพื่อใช้ Token ของ semantic-release แทน

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create Release
        uses: cycjimmy/semantic-release-action@v4
        # ใช้ Action ตัวนี้แทน official action เพราะจัดการ plugin ง่ายกว่ามาก
        # ไม่ต้องมานั่ง install plugin เองใน step แยก
        with:
          semantic_version: 25
          extra_plugins: |
            @semantic-release/changelog
            @semantic-release/git
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

---

## ผลลัพธ์ที่ได้

เมื่อคุณตั้งค่าเสร็จแล้ว ลองแก้โค้ดและ Commit ดูครับ

1.  **Commit:** `git commit -m "feat: add awesome animation to home page"`
2.  **Push:** `git push origin main`

จากนั้นไปดูที่ Tab **Actions** ใน GitHub ของคุณ คุณจะเห็น Workflow `Release` กำลังทำงาน

เมื่อเสร็จสิ้น สิ่งที่จะเกิดขึ้นคือ:
1.  **GitHub Release:** จะมี Release ใหม่โผล่ขึ้นมาพร้อมเลขเวอร์ชันใหม่ (เช่น v1.1.0)
2.  **Release Notes:** ใน Release นั้นจะมีรายการบอกว่า "🚀 New Features: add awesome animation..."
3.  **Source Code:** ไฟล์ `package.json` ใน Repo ของคุณจะถูกอัปเดตเลขเวอร์ชัน และไฟล์ `CHANGELOG.md` จะมีประวัติเพิ่มเข้ามา
4.  **Bot Commit:** จะมี Commit ใหม่จากบอทชื่อ `chore(release): 1.1.0 [skip ci]` ปรากฏขึ้นมา

---

## ข้อดี vs ข้อสังเกต

<ProsCons>
  <div slot="pros">
    <h4>ข้อดี</h4>
    <ul>
      <li><strong>ประหยัดเวลา:</strong> ลดงาน Manual ที่น่าเบื่อและผิดพลาดง่าย</li>
      <li><strong>ความเป็นมืออาชีพ:</strong> โปรเจกต์ดูดีมีมาตรฐาน มี Changelog ชัดเจน</li>
      <li><strong>บังคับวินัย:</strong> ทำให้ทีมต้องใส่ใจกับการเขียน Commit Message ให้สื่อความหมาย</li>
      <li><strong>Traceability:</strong> ย้อนดูได้ง่ายว่าฟีเจอร์นี้เข้ามาในเวอร์ชันไหน</li>
    </ul>
  </div>
  <div slot="cons">
    <h4>ข้อสังเกต</h4>
    <ul>
      <li><strong>Learning Curve:</strong> ทีมต้องเรียนรู้และปรับตัวกับการเขียน Conventional Commits</li>
      <li><strong>Config ซับซ้อนในตอนแรก:</strong> การตั้งค่า Plugins และ Workflow อาจดูยุ่งยากสำหรับมือใหม่</li>
      <li><strong>Sensitive:</strong> ถ้าเขียน Commit ผิด Type เวอร์ชันอาจจะขยับผิดได้ (แต่แก้ไขได้ด้วยการ Revert)</li>
    </ul>
  </div>
</ProsCons>

## สรุป

การทำ **Semantic Release** อาจจะดูเหมือนเป็นการเพิ่มขั้นตอนในช่วงแรกที่ต้องมานั่ง Set up ระบบ แต่ในระยะยาวแล้ว มันคือ **การลงทุนที่คุ้มค่าที่สุด** อย่างหนึ่งสำหรับ Developer ครับ มันช่วยคืนเวลาให้เราไปโฟกัสกับการเขียนโค้ดที่มีคุณภาพ แทนที่จะต้องมานั่งกังวลเรื่องงานเอกสารและงานรูทีน

ลองนำไปปรับใช้กับโปรเจกต์ของคุณดู แล้วคุณจะไม่อยากกลับไปทำ Manual Release อีกเลย